[package]
name = "atoma-backends"
version.workspace = true
edition.workspace = true

[dependencies]
atoma-vllm-backend = { path = "vllm" }
serde = { workspace = true, features = ["derive"] }

[features]
cuda = ["atoma-vllm-backend/cuda"]
nccl = ["cuda", "atoma-vllm-backend/nccl"]
