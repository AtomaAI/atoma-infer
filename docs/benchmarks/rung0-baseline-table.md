# Rung-0 baseline table

**No run yet.** This file is written by `atoma-bench compare --table`; until the rung-0 H100 stint
runs, there is nothing in it. It holds numbers only — the protocol, the pinned versions and the
procedure are in [rung0-baseline.md](rung0-baseline.md), which is hand-written and never
overwritten by the tool.
